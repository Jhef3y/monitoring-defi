<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!-- Spring Boot 4.0 (Spring Framework 7) e a primeira linha com suporte de
             primeira classe ao Java 25: ASM atualizado no plugin de repackage e
             geracao de proxies compativel com bytecode major 69. -->
        <version>4.0.6</version>
        <relativePath/>
    </parent>

    <groupId>com.defi</groupId>
    <artifactId>monitoring-defi</artifactId>
    <version>1.0.0</version>
    <name>monitoring-defi</name>
    <description>Pipeline ETL de alta performance para monitorar liquidez concentrada (Uniswap V3) e popular TimescaleDB</description>

    <properties>
        <!-- Java 25 (LTS). Structured Concurrency (JEP 505) ainda e preview no 25,
             por isso habilitamos o modo preview no compilador (ver build/plugins).
             Virtual Threads, Records e Pattern Matching for switch ja sao finais. -->
        <java.version>25</java.version>
        <maven.compiler.release>25</maven.compiler.release>
    </properties>

    <dependencies>
        <!-- Web + WebClient (The Graph GraphQL) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webflux</artifactId>
        </dependency>

        <!-- WebSocket client (Alchemy RPC stream) -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-websocket</artifactId>
        </dependency>

        <!-- Persistência JDBC para TimescaleDB/Postgres -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-jdbc</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
        </dependency>

        <!-- Flyway para as migrações (hypertable).
             IMPORTANTE: no Spring Boot 4 o Flyway NÃO é mais auto-configurado só
             com flyway-core/flyway-database-postgresql — é preciso o starter
             dedicado, que traz a auto-configuração + flyway-core. O módulo
             postgresql é mantido explícito para o suporte específico ao Postgres. -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <!-- Agendamento + métricas -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <release>25</release>
                    <compilerArgs>
                        <arg>--enable-preview</arg>
                    </compilerArgs>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <configuration>
                    <argLine>--enable-preview</argLine>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
