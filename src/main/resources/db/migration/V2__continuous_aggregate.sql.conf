# Configuração de script do Flyway para a V2.
# Continuous aggregates e jobs de background do TimescaleDB não podem rodar
# dentro de uma transação — desligamos a execução transacional deste script.
executeInTransaction=false
